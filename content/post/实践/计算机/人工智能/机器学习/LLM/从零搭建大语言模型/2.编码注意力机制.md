---
title: "编码注意力机制"
description: ""
slug: "编码注意力机制"
date: 2025-09-15 00:00:00+0000
categories:
    - 计算机
tags:
    - LLM
weight: 1
---

## 2.1 长序列建模的问题
- 不同语言之间的语法规则不同，导致语序不同
- 在transformer出现之前一直使用RNN的方式解决该问题，在神经网络之中加入隐藏层来解析出其数据之间的关系

<img src="https://sebastianraschka.com/images/LLMs-from-scratch-images/ch03_compressed/04.webp" width="500px">


## 2.2 通过注意力机制来捕捉数据间的依赖性
- 注意力机制能够更关注某些标签，使那些更重要的标签受到关注
- transformer的自注意机制，是一种能够让序列里的其他位置来确定某位置的相关性的技术。


## 2.3 通过注意力机制来关注输入的不同部分

### 2.3.1 一种简单的没有可训练权重的自注意机制
<img src="https://sebastianraschka.com/images/LLMs-from-scratch-images/ch03_compressed/07.webp" width="400px">
- 目的：为每一个元素 $x_{i}$ 计算出一个具有相同维度的上下文向量 $z_{i}$
	- $z_{i}$ 是 $x$ 的加权和
		- 比如 $z_{2}$ 是 $x_{2}$ 相对于其他元素的 $x$ 的加权和
		- 首先我们需要计算其他元素相对于 $x_{2}$ 非规范权重 $\omega_{2,i}$
			- 简化模型中我们让 $q_{2}=x_{2}$
			- $\omega_{2,i} = x_{i} \cdot q_{2}^T$
		- 然后将权重规范化成 $\alpha_{2,i}$
			- 一般使用torch自带的规范化函数


### 2.3.2 将自注意机制扩展出可训练参数
- 我们引入3个可训练的权值矩阵：$W_{q},W_{k},W_{v}$
	- $Q_{2} = x_{2} \cdot W_{q}$
	- $K=x \cdot W_{k}$
	- $\omega_{2,i}=K_{i} \cdot Q_{2}$
	- 规范化
	- $\alpha_{2,i}=w_{2} \cdot W_{v}$


### 2.3.3 将自注意机制改成因果自注意机制
- 我们需要使用遮罩将注意力权重矩阵对角线的上半部分隐藏，使得模型做出的预测都是基于前面的单词
- <img src="https://sebastianraschka.com/images/LLMs-from-scratch-images/ch03_compressed/19.webp" width="800px">
- 用负无穷将隐藏部分覆盖

### 2.3.4 使用 dropout 随机隐藏掉一些地方来避免过拟合
- <img src="https://sebastianraschka.com/images/LLMs-from-scratch-images/ch03_compressed/22.webp" width="400px">

- 一般的丢包设置在规范化之后，丢包率为0.1到0.2
```python
class CausalAttention(nn.Module):  
  
    def __init__(self, d_in, d_out, context_length,  
                 dropout, qkv_bias=False):  
        super().__init__()  
        self.d_out = d_out  
        self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)  
        self.W_key   = nn.Linear(d_in, d_out, bias=qkv_bias)  
        self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)  
        self.dropout = nn.Dropout(dropout) # New  
        self.register_buffer('mask', torch.triu(torch.ones(context_length, context_length), diagonal=1)) # New  
  
    def forward(self, x):  
        b, num_tokens, d_in = x.shape # New batch dimension b  
        # For inputs where `num_tokens` exceeds `context_length`, this will result in errors        # in the mask creation further below.        # In practice, this is not a problem since the LLM (chapters 4-7) ensures that inputs        # do not exceed `context_length` before reaching this forward method.   
keys = self.W_key(x)  
        queries = self.W_query(x)  
        values = self.W_value(x)  
  
        attn_scores = queries @ keys.transpose(1, 2) # Changed transpose  
        attn_scores.masked_fill_(  # New, _ ops are in-place  
            self.mask.bool()[:num_tokens, :num_tokens], -torch.inf)  # `:num_tokens` to account for cases where the number of tokens in the batch is smaller than the supported context_size  
        attn_weights = torch.softmax(  
            attn_scores / keys.shape[-1]**0.5, dim=-1  
        )  
        attn_weights = self.dropout(attn_weights) # New  
  
        context_vec = attn_weights @ values  
        return context_vec  
  
torch.manual_seed(123)  
  
context_length = batch.shape[1]  
ca = CausalAttention(d_in, d_out, context_length, 0.0)  
  
context_vecs = ca(batch)  
  
print(context_vecs)  
print("context_vecs.shape:", context_vecs.shape)
```

## 2.4 将单头注意力扩展到多头注意力

- <img src="https://sebastianraschka.com/images/LLMs-from-scratch-images/ch03_compressed/24.webp" width="800px">
- <img src="https://sebastianraschka.com/images/LLMs-from-scratch-images/ch03_compressed/25.webp" width="400px">

### 2.4.1 通过权重分割实现多头注意力
