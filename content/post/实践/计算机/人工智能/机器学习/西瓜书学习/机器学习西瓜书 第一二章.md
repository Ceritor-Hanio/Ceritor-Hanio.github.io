---
title: "机器学习西瓜书 第一二章"
description: ""
slug: "机器学习西瓜书第一二章"
date: 2025-09-15 00:00:00+0000
categories:
    - 计算机
tags:
    - 机器学习
weight: 1
---

# 第一章·绪论
## 1.1 引言
机器学习：在计算机上从数据产生“模型”，即“学习算法”
## 1.2 术语
1. 数据集 $D={x_{1},x_{2},\dots,x_{m}}$
    1. 样本（示例），**特征向量** $x_{i}=(x_{i,1},x_{i,2},\dots ,x_{i,d})$
        1. 属性 
    2. 样本空间 $d$ 维的 $\chi$
    3. 
2. 训练集 $\stackrel{学习}{\to}$ 假设，目的是通过学习找到或逼近**真相**
3. 预测的类型
	1. 分类（离散值）
	2. 回归（连续值）
4. 聚类学习
	1. 分组，形成很多簇
5. 泛化能力
	学习的模型适应于新样本的能力

## 1.3 假设空间

1. 概念学习
	“从样例中学习”
	- 狭义
		从训练数据中学得概念（理论研究困难，现代常用技术都是黑箱）
		- 布尔概念学习
			- 是不是的问题
			- 学习过程指在 **假设空间** 进行搜索
			- 可能有多个假设与同个训练集一致，将假设集合称为 “**版本空间**”

## 1.4 归纳偏好

机器学习只能得到一个假设，某种机器学习算法可能偏好某种类型的假设。

**奥卡姆剃刀法则**
	选择最简单的假设！

**NFL定理**
	对于所有问题同等重要的情形，所有学习算法的期望性能相同。**要关注具体的问题，考虑不同项目对于具体问题的优劣性**
# 第二章·模型评估与选择

## 2.1 经验误差与过拟合
1. 错误率
    预测错误的样本数占样本总数的比例
    1. 精度 = 1 - 错误率
    2. **误差** 预测输出与真实输出的差值
        **训练误差** 在训练集上产生的误差
        **泛化误差** 在新样本上
        在不知道新样本的时候，只能尽量降低训练误差，但是训练误差不能太小
    3. **过拟合、欠拟合**
        过拟合：将特殊规律当成一般规律
        欠拟合：未学好一般规律

## 2.2 评估方法
通过处理方法，将数据集 $D$ 处理为尽量互斥的训练集 $S$ 和测试集 $T$

### 2.2.1 留出法
直接划分成两个互斥的集合
- 应该采用分层采样
    - 尽量保持数据分布的一致性
- 分层采样也有多种划分
    - 通常使用多次随机划分，然后对得到的所有结果取平均
- 但是训练集和测试集都不能太多或太小

### 2.2.2 交叉验证法
- 将数据集分成 $k$ 个互斥子集，每次留一个作为测试集，剩下的作为训练集。称为 $k$ 折交叉验证
- 对于同一个数据集也可能有很多种不同的分法，可以称为 $m$ 次 $k$ 折交叉验证

### 2.2.3 自助法
设数据集 $D$ 中有 $m$ 个样本，每次选从数据集 $D$ 中随机选一个复制到 训练集$D'$,选 $m$ 次。

则我们可以得到一个数据个数为 $m$ 的测试集，且因为 $\lim_{ m \to \infty }\left( 1-\frac{1}{m} \right)^{m}=\frac{1}{e} \approx 0.368$ 则数据集中有一部分样本不在测试集中，可以作为测试集。

优缺点：
- 在数据集较小时很有用，且能产生多个不同的训练集
- 改变了原始数据的分布，有偏差。

### 2.2.4 调参与最终模型
大部分学习算法都需要设定参数，不同参数对于训练出来的最终模型有很大影响。

没办法使用参数枚举，于是一般都是先规定范围，再设定步长来局部枚举。

但是有些大型深度学习可能有上亿个参数，所以也很难用枚举法。

## 2.3 性能度量
性能度量反应了任务需求，模型的好坏不仅要考虑算法和数据，并且是相对于任务需求而言的。

- 回归任务中常用均方误差
    - 对于样例集 $D=\{ (x_{1},y_{1}),(x_{2},y_{2}),\dots,(x_{m},y_{m})\}$,均方误差就是 $$
E(f;D)=\frac{1}{m}\sum_{i = 1}^{m}(f(x_{{i}},y_{i}))
$$

### 2.3.1 错误率与精度
- 错误率
    - $E(f;D)=\frac{1}{m} \sum _{i=1}^{m}\mathbb{I} (f(x_{i})\neq y_{i})$
- 精度
    - $E(f;D)=\frac{i}{m}\sum_{i=1}^{m} \mathbb{I}(f(x_{i})=y_{i})$

## 2.3.2 查准率、查全率与 $F1$
我们除了关心正确与错误的情况，还关心==被判断为正确的有多少是真的正确==，和==有多少正确的被找了出来==。

 于是就有查准率和查全率两个概念，用来描述