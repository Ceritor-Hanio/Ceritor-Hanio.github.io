---
title: 电控组考核项目完成流程
description: 
slug: 电控组考核项目完成流程
date: 2025-09-15 00:00:00+0000
categories:
    - 计算机
tags:
    - 单片机
weight: 1
---
# 1. 环境配置和单片机连接
1. 连接所有需要用到的导线
	1. 连接STlink（注意，此处STlink要连接下面的引脚！）**图片来源：江协科技**
	![2-1 工程模板.jpg](https://gitee.com/Dragon-chen/images/raw/master/image/202411141907571.jpg)
	
	1. 连接OLED和旋钮
		![5-2 旋转编码器计次.jpg](https://gitee.com/Dragon-chen/images/raw/master/image/202411141908324.jpg)

	3. 连接LED小灯泡
 ![6-3 PWM驱动LED呼吸灯.jpg](https://gitee.com/Dragon-chen/images/raw/master/image/202411141908417.jpg)
2. 完成连接之后，开始配置环境。
	1. 安装软件
		1. 直接上官网安装就行。
		2. 安装器件支持包
			1. 提前下载好，离线安装支持包就行
			2. 在keil，点击魔术棒右边的最靠右的绿色按钮，搜索并安装对应芯片的器件支持包
		3. 软件注册
			略。具体看江协科技视频
		4. STlink插件安装
			1. 插上STlink
			2. 打开keil的安装目录，在ARM\STlink\USBDriver\,双击dpinst
3. 新建工程
	1. 打开keil，project/new project，选择工程位置，命名
	2. 选择STMF103C8T6
	3. 在工程文件夹新建Start文件，复制粘贴stm32的启动文件和外设寄存器描述文件，内核寄存器描述文件
	4. 在keil中点击三个小盒子的图标，添加添加Group（刚才新建的Start文件），然后添加文件（后缀为md.s和其他.c和.h的文件)
	5. 在魔术棒中选择C，添加路径，将刚才的start文件添加进去。
	任何文件都要添加进group和C的编译路径。
	6. 添加User文件，建立main.c文件
	7. 点击魔术棒，
		1. 将ARM compiler 改成 vesion 5.（stm32不支持太高阶的编译器）
		2. 选择Debug，Use改为STlink，然后点击设置，在flash downlo 勾选 Reset and Run
	8. 添加library和delay文件夹
# 2. oled显示
1. 新建hardware文件夹，添加江协科技所提供的'OLED.c','OLED.H','OLED_Font.h'文件，操作如上，要添加group和魔术棒里面哦。

# 3.旋钮计数
1. 向hardware里面添加'Encoder.c','Encoder.h'
代码如下：
```c
#include "stm32f10x.h"                  // Device header

int16_t Encoder_Count;
void Encoder_Init(void){//初始化函数
	
	//开启时钟
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB,ENABLE);
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO,ENABLE);
	
	//初始化GPIO
	GPIO_InitTypeDef GPIO_InitStructure;
	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_1|GPIO_Pin_0;//连接的通路是B0和B1
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_Init(GPIOB, &GPIO_InitStructure);
  
 //配置中断引脚
	GPIO_EXTILineConfig(GPIO_PortSourceGPIOB,GPIO_PinSource0);	
	GPIO_EXTILineConfig(GPIO_PortSourceGPIOB,GPIO_PinSource1);
	
	//初始化中断
	EXTI_InitTypeDef EXTI_InitStructure;
	EXTI_InitStructure.EXTI_Line=EXTI_Line0|EXTI_Line1;
	EXTI_InitStructure.EXTI_LineCmd = ENABLE;
	EXTI_InitStructure.EXTI_Mode = EXTI_Mode_Interrupt;
	EXTI_InitStructure.EXTI_Trigger = EXTI_Trigger_Falling;
	EXTI_Init(&EXTI_InitStructure);
	
	//用NVIC管理中断优先级
	NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);
	
	NVIC_InitTypeDef NVIC_InitStructure;						
	NVIC_InitStructure.NVIC_IRQChannel = EXTI0_IRQn;		
	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;			
	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 1;//设定两条通路的中断优先级，此处为最高级。	
	NVIC_InitStructure.NVIC_IRQChannelSubPriority = 1;
	NVIC_Init(&NVIC_InitStructure);	
	
	NVIC_InitStructure.NVIC_IRQChannel = EXTI1_IRQn;
	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;
	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 1;
	NVIC_InitStructure.NVIC_IRQChannelSubPriority = 2;
	NVIC_Init(&NVIC_InitStructure);	
}
int16_t Encoder_Get(void){//获取中断次数
	int16_t Temp;
	Temp = Encoder_Count;
	Encoder_Count = 0;
	return Temp;
}
//对两个方向引起的中断进行分别计数
void EXTI0_IRQHandler(void){
		if(EXTI_GetFlagStatus(EXTI_Line0) == SET){
			if (GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_0) == 0)
		{
			if (GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_1) == 0)		
			{
				Encoder_Count --;					
			}
		}
			EXTI_ClearITPendingBit(EXTI_Line0);
			
		}
}

void EXTI1_IRQHandler(void){
		if(EXTI_GetFlagStatus(EXTI_Line1) == SET){
			if (GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_1) == 0)
		{
			if (GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_0) == 0)		
			{
				Encoder_Count ++;					
			}
		}
			EXTI_ClearITPendingBit(EXTI_Line1);
			
		}
}
```

Encoder.h 只是声明函数而已。不赘述。
# 4. PWM控制呼吸灯
PWM波就是高低电平交替输出形成的波形，用来控制电子设备十分好用。

波的特性
- 周期：$ARR+1$ ，用计数周期代替波的周期
- 频率：$\frac{CK\_ PSC/(PSC+1)}{ARR+1}$，CK_PSC是时钟频率，PSC是预分频器，
- 占空比：$\frac{CCR}{ARR+1}$，CCR是比较输出寄存器的值，按照比较方式进行比较并输出高低电平。
- ==分辨率：==$\frac{1}{ARR+1}$ （其实我没太搞懂分辨率是啥）
假设我们要输出一个特定频率、特定占空比、特定分辨率的PWM波，就可以通过解方程计算如何定制参数。

编写TiMer，实现PWM波。
```c
#include "stm32f10x.h"                  // Device header

void PWM_Init(void){
	//打开定时器和GPIO口的时钟
	RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2,ENABLE);
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE);
	
	GPIO_InitTypeDef GPIO_InitStructure;
	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF_PP;//复用推挽输出，用于输出PWM波
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_0;
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_Init(GPIOA,&GPIO_InitStructure);
	
	
	//配置时基单元
	TIM_InternalClockConfig(TIM2);
	
	TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;
	TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;
	TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up;
	TIM_TimeBaseInitStructure.TIM_Period = 100 - 1;//ARR，计数周期
	TIM_TimeBaseInitStructure.TIM_Prescaler = 720 - 1;//PSC，预分频器
	TIM_TimeBaseInitStructure.TIM_RepetitionCounter = 0;
	TIM_TimeBaseInit(TIM2,&TIM_TimeBaseInitStructure);
	
	// 配置比较输出模式
	TIM_OCInitTypeDef TIM_OCInitStructure;
	TIM_OCStructInit(&TIM_OCInitStructure);
	TIM_OCInitStructure.TIM_OCMode = TIM_OCMode_PWM1;
	TIM_OCInitStructure.TIM_OCPolarity = TIM_OCPolarity_High;
	TIM_OCInitStructure.TIM_OutputState = TIM_OutputState_Enable;
	TIM_OCInitStructure.TIM_Pulse = 0;
	TIM_OC1Init(TIM2,&TIM_OCInitStructure) ;
	
	//打开定时器
	TIM_Cmd(TIM2,ENABLE);
}
void PWM_SetCompare1(uint16_t Compare)
{
	TIM_SetCompare1(TIM2, Compare);		//设置CCR1的值，CCR越大则占空比越大，灯越亮
}
```

主文件
```cpp
#include "stm32f10x.h"                  // Device header
#include "Delay.h"
#include "OLED.h"
#include "Encoder.h"
#include "PWM.h"

uint8_t Num;

int main(void)
{
	OLED_Init();
	PWM_Init();
	Encoder_Init();//上面全是初始化
	while(1){
		Num += Encoder_Get() * 2;
		OLED_ShowSignedNum(1,5,Num,5);//屏幕显示数字
		PWM_SetCompare1(Num);//利用数字控制灯			
		Delay_ms(10);	
	}
	
}	

```